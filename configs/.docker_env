DOCKER_HOST_PORT=8080 # порт для доступа к приложению для хоста

DOCKER_HOST_DB_PORT=9388 # порт для доступа к бд для хоста

DOCKER_HOST_REDIS_PORT=6378 # порт для доступа к редису для хоста

LOGDIR=../logdir # в приложении есть папка с логами, конкретно это путь до папки где эти логи будут на хосте